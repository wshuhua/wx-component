
<view wx:if="{{mask}}" class="mask {{show ? '' : 'mask_hidden'}} {{maskClass}}" bindtap="closeActionSheet"></view>
<view class="actionsheet {{show ? 'actionsheet_toggle' : ''}} {{extClass}}">
    <!-- 标题 -->
    <slot></slot>
</view>

