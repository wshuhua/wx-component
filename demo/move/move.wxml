<wxs module="move" src="./move.wxs"></wxs>
<view class="page">
    <view class="box" bindtouchstart="{{move.touchstart}}" bindtouchmove="{{move.touchmove}}">
        <image class="img" src="{{url}}" alt="" />
    </view>
    <view class="btn" bindtap="onSelectImage">选择图片</view>
</view>